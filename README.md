# v-films-home-page (placeholder assets)

This is a ready-to-run React + Vite + Tailwind project (placeholder images).
Replace the files under `src/assets/` with your Figma/Anima exported images for the final look.

Run locally:
```bash
npm install
npm run dev
```

Contact form posts to: https://vernanbackend.ezlab.in/api/contact-us/
(Replace/remove during testing if backend blocks CORS)
